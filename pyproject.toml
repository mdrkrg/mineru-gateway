[project]
name = "mineru-gateway"
version = "0.1.0"
description = "API-Key gateway in front of mineru-api / mineru-router"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "aiofiles>=24.1.0",
    "aiosqlite>=0.22.1",
    "alembic>=1.18.5",
    "fastapi>=0.139.0",
    "fastapi-users[oauth,sqlalchemy]>=15.0.5",
    "httpx>=0.28.1",
    "pydantic-settings>=2.14.2",
    "python-multipart>=0.0.32",
    "sqlalchemy[asyncio]>=2.0.51",
    "uvicorn[standard]>=0.51.0",
]

[project.scripts]
mineru-gateway = "mineru_gateway.main:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/mineru_gateway"]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
pythonpath = ["."]
markers = [
    "e2e: end-to-end tests against real HTTP (subprocess gateway + mock upstream)",
    "real_upstream: tests that exercise real mineru-api/mineru-router when available, fall back to mock",
]

[dependency-groups]
dev = [
    "anyio>=4.14.1",
    "asgi-lifespan>=2.1.0",
    "freezegun>=1.5.5",
    "locust>=2.46.2",
    "pre-commit>=4.6.0",
    "psutil>=7.0.0",
    "pytest>=8.0",
    "pytest-asyncio>=1.4.0",
    "pytest-cov>=7.1.0",
    "ruff>=0.15.21",
]
postgres = [
    "asyncpg>=0.31.0",
]

[tool.ruff]
exclude = ["alembic/versions"]
